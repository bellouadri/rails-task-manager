<h2><%= @task.title %></h2>

<p><%= @task.details %></p>

<p>Completed : <%= @task.completed %></p>

<%= link_to "Edit the task", edit_path(@task) %>

<%= link_to 'Delete the task', task_path(@task), method: :delete, data: { confirm: "Sure bro ?"} %>

<%= link_to "Display all tasks", tasks_path %>
