<h1>Tasks to do</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %>
      | <%= link_to 'Delete', task_path(task), method: :delete, data: { confirm: "Sure bro ?"} %>
    </li>

  <% end %>
</ul>

<%= link_to "Create a new task", new_path %>
